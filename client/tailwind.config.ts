import type { Config } from "tailwindcss"

export default {
  darkMode: ["class"],
  // Tailwind CSS v4 with @tailwindcss/vite doesn't require content globs here.
  theme: {
    extend: {},
  },
  plugins: [],
} satisfies Config
